<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="edu.fandm.enovak.updatetimingcollector.Main">

    <TextView android:id="@+id/main_tv_explain"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="The app keeps a log of application updates.  The only information it stores is the application name, the version, and the date each time an app is updated or installed.  You can view the log anytime!"
        android:padding="20dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:textAlignment="center"/>

    <Button android:id="@+id/main_button_view_log"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/main_tv_explain"
        android:layout_marginTop="20dp"
        android:text="View Log"
        android:onClick="launchLogViewActivity"/>

</android.support.constraint.ConstraintLayout>
